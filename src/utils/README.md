# Utilities

The `/src/utils` directory contains purely functional, stateless helper methods that are largely agnostic to the core business domain.

Included are modules for parsing CSV/XML/JSON files into standard rows, normalizing Indonesian formatting for numbers/currencies/Text, handling deep hash generation, cryptographic operations, and managing standardized Date objects for querying datasets.
