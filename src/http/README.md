# HTTP Helpers

The `/src/http` directory provides utilities for constructing and standardizing HTTP requests and responses.

This includes wrappers for standardizing JSON API responses, resolving canonical error messages for public viewing, issuing Auth Tokens (JWTs), and rate limiting.
