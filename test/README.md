# Test Suites

The `/test` directory is aligned with Node.js built-in `node:test` runner.

It contains modular unit tests and integration tests mocking database operations, LLM function calling endpoints, and parsing logic.
Run the suite exclusively through `npm test`.
