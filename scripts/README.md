# Operational Scripts

The `/scripts` directory contains CLI operations and standalone functional tasks intended for operators and developers.

Use these to perform database migrations, backup/restore logic, cache invalidation, or quick tenant bootstrapping without hitting HTTP routes.
